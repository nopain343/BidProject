<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bidMapper">
	<select id="loginUser" parameterType="userInfo" resultType="userInfo">
		<![CDATA[
			SELECT * FROM USER1 WHERE ID=#{id} AND PASSWORD=#{password}
		]]>
	</select>
	
	
	<!-- <insert id="insertUser" parameterType="com.user.vo.UserVO">
		<![CDATA[
			INSERT INTO USER(ID, PASSWORD, ADDRESS, PHONE, NICKNAME)
			VALUES (#{id}, #{password}, #{address}, #{phone}, #{nickname})
		]]>
	</insert>
	
	<delete id="deleteUser" parameterType="java.lang.String">
		<![CDATA[
			DELETE FROM USER
			WHERE NAME = #{id}
		]]>
	</delete>
	
	<update id="updateUser" parameterType="com.user.vo.UserVO">
		<![CDATA[
			UPDATE USER SET
			PASSWORD = #{password}, ADDRESS = #{address}, PHONE = #{phone}, NICKNAME = #{nickname}
			WHERE ID = #{id}
		]]>
	</update> -->

</mapper>