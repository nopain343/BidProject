<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bidMapper">
	<select id="loginUser" parameterType="userInfo" resultType="userInfo">
		<![CDATA[
			SELECT * FROM USER1 WHERE ID = #{id} AND PASSWORD = #{password}
		]]>
	</select>
	
	
	<insert id="insertUser" parameterType="userInfo">
		<![CDATA[
			INSERT INTO USER1(ID, PASSWORD, USERNAME, ROADFULLADDR, PHONE, PWQUESTION, PWANSWER)
			VALUES (#{id}, #{password}, #{username}, #{roadFullAddr}, #{phone}, #{pwquestion}, #{pwanswer})
		]]>
	</insert>
	
 	<delete id="deleteUser" parameterType="java.lang.String">
		<![CDATA[
			DELETE FROM USER1
			WHERE ID = #{id}
		]]>
	</delete>
	
	<update id="updateUser" parameterType="com.user.vo.UserVO">
		<![CDATA[
			UPDATE USER1 SET
			PASSWORD = #{password}, USERNAME = #{username}, ROADFULLADDR = #{roadFullAddr}, PHONE = #{phone}, PWQUESTION = #{pwquestion}, PWANSWER = #{pwanswer}
			WHERE ID = #{id}
		]]>
	</update>

</mapper>