<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="auctionMapper">
	<select id="view" parameterType="java.lang.String" resultType="categoryVO">
		<![CDATA[
			SELECT * FROM SALES WHERE CODE = #{code}
		]]>
	</select>
	
	<select id="maxPrice" parameterType="java.lang.String" resultType="BidVO">
		<![CDATA[
			SELECT FINALPRICE, TERM FROM AUCTION WHERE FINALPRICE = (SELECT MAX(FINALPRICE) AS FINALPRICE FROM AUCTION WHERE CODE = #{code}) AND CODE = #{code}
		]]>
	</select>
	
	<insert id="bidPlaceA" parameterType="BidVO">
		<![CDATA[
			INSERT INTO AUCTION(ID, CODE, TERM, FINALPRICE, CAT1) VALUES (#{id}, #{cat1}||CODE_A.NEXTVAL, #{term}, #{finalPrice}, #{cat1})
		]]>
	</insert>
	
	<insert id="bidPlaceB" parameterType="BidVO">
		<![CDATA[
			INSERT INTO AUCTION(ID, CODE, TERM, FINALPRICE, CAT1) VALUES (#{id}, #{cat1}||CODE_B.NEXTVAL, #{term}, #{finalPrice}, #{cat1})
		]]>
	</insert>
	
	<insert id="bidPlaceC" parameterType="BidVO">
		<![CDATA[
			INSERT INTO AUCTION(ID, CODE, TERM, FINALPRICE, CAT1) VALUES (#{id}, #{cat1}||CODE_C.NEXTVAL, #{term}, #{finalPrice}, #{cat1})
		]]>
	</insert>
	
	<insert id="bidPlaceD" parameterType="BidVO">
		<![CDATA[
			INSERT INTO AUCTION(ID, CODE, TERM, FINALPRICE, CAT1) VALUES (#{id}, #{cat1}||CODE_D.NEXTVAL, #{term}, #{finalPrice}, #{cat1})
		]]>
	</insert>
	
	<insert id="bidPlaceE" parameterType="BidVO">
		<![CDATA[
			INSERT INTO AUCTION(ID, CODE, TERM, FINALPRICE, CAT1) VALUES (#{id}, #{cat1}||CODE_E.NEXTVAL, #{term}, #{finalPrice}, #{cat1})
		]]>
	</insert>
	
	<insert id="bidPlaceF" parameterType="BidVO">
		<![CDATA[
			INSERT INTO AUCTION(ID, CODE, TERM, FINALPRICE, CAT1) VALUES (#{id}, #{cat1}||CODE_F.NEXTVAL, #{term}, #{finalPrice}, #{cat1})
		]]>
	</insert>
	
	<insert id="bidPlaceG" parameterType="BidVO">
		<![CDATA[
			INSERT INTO AUCTION(ID, CODE, TERM, FINALPRICE, CAT1) VALUES (#{id}, #{cat1}||CODE_G.NEXTVAL, #{term}, #{finalPrice}, #{cat1})
		]]>
	</insert>
	
	<insert id="bidPlaceH" parameterType="BidVO">
		<![CDATA[
			INSERT INTO AUCTION(ID, CODE, TERM, FINALPRICE, CAT1) VALUES (#{id}, #{cat1}||CODE_H.NEXTVAL, #{term}, #{finalPrice}, #{cat1})
		]]>
	</insert>
	
	<!--
 	<delete id="deleteUser" parameterType="java.lang.String">
		<![CDATA[
			DELETE FROM USER1
			WHERE ID = #{id}
		]]>
	</delete>
	
	<update id="updateUser" parameterType="userInfo">
		<![CDATA[
			UPDATE USER1 SET
			PASSWORD = #{password}, USERNAME = #{username}, ROADFULLADDR = #{roadFullAddr}, PHONE = #{phone}
			WHERE ID = #{id}
		]]>
	</update>
	
	<select id="searchPassword2" parameterType="userInfo" resultType="userInfo">
		<![CDATA[
			SELECT PASSWORD, PWQUESTION, PWANSWER FROM USER1 WHERE ID = #{id}
		]]>
	</select>
	
	<select id="searchId2" parameterType="userInfo" resultType="userInfo">
		<![CDATA[
			SELECT ID, USERNAME, PHONE FROM USER1 WHERE USERNAME = #{username} AND PHONE = #{phone}
		]]>
	</select> -->
	
</mapper>